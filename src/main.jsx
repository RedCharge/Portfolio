// main.jsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import Home from './home.jsx' // Make sure this path is correct


ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Home />
  </React.StrictMode>,
)